import { getAnonTokenClientSide } from "@utils/index";
import ALL_WORDS from "@data/we.json";

const Game = () => {
  let token = getAnonTokenClientSide();
  return <div>Game</div>;
};

export default Game;
